"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
exports.__esModule = true;
var nock_1 = __importDefault(require("nock"));
var tools_1 = require("./tools");
var createJWKSMock = function (jwksOrigin, jwksPath) {
    if (jwksPath === void 0) { jwksPath = '/.well-known/jwks.json'; }
    var keypair = (0, tools_1.createKeyPair)();
    var JWKS = (0, tools_1.createJWKS)(__assign(__assign({}, keypair), { jwksOrigin: jwksOrigin }));
    var jwksUrlInterceptor;
    var kid = function () { return JWKS.keys[0].kid; };
    var start = function () {
        jwksUrlInterceptor = (0, nock_1["default"])(jwksOrigin).get(jwksPath);
        jwksUrlInterceptor.reply(200, JWKS).persist();
    };
    var stop = function () {
        if (jwksUrlInterceptor) {
            nock_1["default"].removeInterceptor(jwksUrlInterceptor);
        }
    };
    var token = function (token) {
        if (token === void 0) { token = {}; }
        return (0, tools_1.signJwt)(keypair.privateKey, token, kid());
    };
    return {
        start: start,
        stop: stop,
        kid: kid,
        token: token
    };
};
exports["default"] = createJWKSMock;
